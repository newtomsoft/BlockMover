<table class="grid">
    @for (var y = 0; y < Size; y++)
    {
        <tr>
            @for (var x = 0; x < Size; x++)
            {
                var coordinate = Coordinate.From(x, y);
                var block = Grid.Blocks.FirstOrDefault(block => block.HasCoordinate(coordinate));
                var (freeOrBlock, borders) = GetClasses(block, coordinate);
                var currentX = x;
                var currentY = y;
                <td id="g@(currentX)_@(currentY)" class="@freeOrBlock @borders"></td>
            }
        </tr>
    }
</table>
